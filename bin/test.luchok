num = rnd(31)
play = false
delay_timer = 60
function vblank()   
    if delay_timer == 0 then
        delay_timer = 60
        play = not play
        if play then
            sound_timer = 60
        end
    end
end
